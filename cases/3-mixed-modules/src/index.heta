/*
  This modul imports different types of modules
*/

include json.json oftype json

include heta.heta oftype heta

include yaml.yml oftype yml

// should be replaced by include but how?
#import {
  source: xlsx.xlsx,
  type: xlsx,
  options: {
    sheetNum: [1, 2],
    omitRows: 3
  }
};

// should be replaced by include but how?
#import {
  source: page.md,
  type: md,
  options: {pageId: xxx},
  id: yyy,
  title: hhh
};

yaml @YAMLExport;
