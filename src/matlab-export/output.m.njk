%%%% This code was generated by {{ builderName }}
% {{ options.title + '.' if options.title}}{{ options.notes }}

function out = {{ options.id }}_Output(t, y, flag)

switch (flag)
case 'init'
   %statement;
case '[]'
   out = y;
case 'done'
   %statement for output;
end

end
