%%%% This code was generated by {{ builderName }}
% {{ options.title + '.' if options.title }}{{ options.notes }}

function p = {{ namespace.spaceName }}_param()

p = zeros({{ constants|length + events|length }}, 1);
%%% parameter annotation
{%- for con in constants %}
p({{ loop.index }}) = {{ con.num }}; % {{ con.id }}, {{ con.title }} ({{ con.units }}) {{ con.notes }}
{%- endfor %}
{%- for evt in events %}
p({{ loop.index + constants|length }}) = {{ 1 if evt.switcher.active==true else 0 }}; % {{ evt.switcher.id }}_, {{ evt.switcher.title }} {{ evt.switcher.notes }}
{%- endfor %}

end
